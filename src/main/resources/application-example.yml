
spring:
  application:
    name: Sistema.Barbearia
  datasource:
    url: jdbc:mysql://localhost:3306/barbearia?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root

    password: ${MYSQL_PASSWORD:your-password-here}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: true

